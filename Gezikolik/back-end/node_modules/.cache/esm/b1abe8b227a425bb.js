let fs,path;_27a‍.x([["fileRemover",()=>fileRemover]]);_27a‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_27a‍.w("path",[["default",["path"],function(v){path=v}]]);


const fileRemover = (filename) => {
    fs.unlink(path.join(__dirname,"../uploads",filename), function(err){
        if(err & err.code == "ENOENT"){
            //bulunamayan dosya
            console.log(`File ${filename} bulanamadı, kaldırılacak`);
        }else if(err){
            console.log(`${filename} kaldırırken hata oluştu.`)
        }else{
            console.log(`${filename} kaldırıldı.`)
        }
    } );
}

